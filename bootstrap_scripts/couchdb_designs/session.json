{
    "_id" : "_design/session",
    "_rev" : "2-8518e5e3b542d57cffc8cd4d7abb9fa1",
    "views" : {
        "all" : {
            "map" :  "function(doc) { 
                        if (doc.type == 'session') {
                            emit(doc._id, doc._rev);
                        }
                      }"
        },
        "bydate": {
            "map": "function(doc) {
                        if(doc.type === 'session') {
                            emit(doc.startTime, {'id': doc._id, 'start': doc['Time'], 'end': doc['End Time'], 'tags': doc.tags}); 
                        }
                    }"
        }
    } 
}