{
    "_id" : "_design/session",
    "views" : {
        "all" : {
            "map" :  "function(doc) { 
                        if (doc.type == 'session') {
                            emit(doc._id, doc._rev);
                        }
                      }"
        },
        "bydate": {
            "map": "function(doc) {
                        if(doc.type === 'session') {
                            emit(doc.startTime, {'id': doc._id, 'start': doc['Time'], 'end': doc['End Time']}); 
                        }
                    }"
        }
    } 
}