{
    "_id" : "_design/session",
    "_rev" : "5-cf7d330e5e88245c96b712248fd47956",
    "views" : {
        "all" : {
            "map" :  "function(doc) { 
                        if (doc.type == 'session') {
                            emit(null, doc);
                        }
                      }"
        },
        "bydate": {
            "map": "function(doc) {
                        if(doc.type === 'session') {
                            emit(doc.startTime, doc); 
                        }
                    }"
        }
    } 
}